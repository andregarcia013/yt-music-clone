import 'package:flutter/material.dart';

class TimerCounter extends StatelessWidget {
  bool isVideo;
  TimerCounter({this.isVideo = false});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
